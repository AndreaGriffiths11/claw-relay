{
  "name": "claw-relay-server",
  "version": "0.1.0",
  "description": "Trust layer between AI agents and agent-browser",
  "main": "src/index.ts",
  "scripts": {
    "start": "bun src/index.ts",
    "dev": "bun --watch src/index.ts",
    "build:dashboard": "cd dashboard && bun install && bun run build",
    "prebuild": "bun run build:dashboard"
  },
  "dependencies": {
    "hono": "^4.0.0",
    "yaml": "^2.3.4"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
